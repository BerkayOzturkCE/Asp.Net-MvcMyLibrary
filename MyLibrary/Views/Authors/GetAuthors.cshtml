@model List<MyLibrary.Application.AuthorOparation.Queries.GetAuthors.AuthorsViewModel>

@{
    ViewBag.Current = "Author";

}
<div class="container ">

    <table class<table class="table table-striped table-bordered table-dark">
            <thead>
                <tr>
                    <th><b>Name</b></th>
                    <th><b>Surname</b></th>
                    <th><b>Birthday</b></th>
                                    <th><b>Operations</b></th>


                </tr>
            </thead>
            <tbody>
                @foreach (var author in Model)
                {
                    <tr>

                    <td>@author.Name</td>
                    <td>@author.Surname</td>
                    <td>@author.Birthday.Date.ToString("d")</td>
                     

                          <td class="text-center">
                            <a class="btn btn-danger" href="">
                                <i class="fa-solid fa-trash"></i>                            
                                <span>Delete</span>
                            </a>
                            @{

                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="d-flex justify-content-md-between">
         <td class="text-center">
                        <a class="btn btn-primary" href="/Authors/AddAuthor">
                            <span>Add Author</span>
                        </a>
                    </td>

</div>

    </div>
